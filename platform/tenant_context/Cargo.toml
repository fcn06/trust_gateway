[package]
name = "tenant_context"
version = "0.1.0"
edition = "2021"
description = "Shared tenant context types for the multi-tenant Agent-in-a-Box platform"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
uuid = { version = "1.0", features = ["v4", "serde"] }
